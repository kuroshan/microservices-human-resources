spring:
  application:
    name: ms-hhrr-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kuroshan/microservices-human-resources
          defaultLabel: develop
          searchPaths: 
            - config/discovery-server
            - config/edge-service
            - config/areas
