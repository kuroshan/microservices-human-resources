spring:
  application:
    name: ms-hhrr-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kuroshan/microservices-human-resources
          searchPaths: config
